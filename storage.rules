rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Permite que cualquiera lea las imágenes
      allow read: if true;
      // Solo usuarios autenticados pueden subir archivos
      allow write: if request.auth != null;
    }
  }
}